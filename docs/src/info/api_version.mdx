---
name: VK API Version
menu: "General info"
route: info/api_version
---

# VK API version

VK API may return different results for different versions.
For example, VK Official method `catalog.getAudio` does not 
return `explore` section with `v=5.116`, but with `v=5.138` does.   