---
name: audio.removeFromPlaylist
menu: "API Methods"
route: method/audio_removeFromPlaylist
---

# audio.removeFromPlaylist

Remove audios from the specified playlist

Parameters:

|Name|Value|
|---|---|
|owner_id|User or community id (for example, obtained by users.get)|
|playlist_id|Id of the playlist|
|audios|List of one or multiple audio ids, separated with ","|

Example (Kate):

```php
//tmpl-kate-common-start
$ownerId = 1;
$playlistId = 1;
$audioIds = '1_12345,1_56789';

curl_setopt(
    $ch,
    CURLOPT_URL,
    "https://api.vk.com/method/audio.removeFromPlaylist?access_token=".TOKEN.
    "&owner_id=$ownerId&playlist_id=$playlistId&audio_ids=".urlencode($audioIds)."&v=5.95"
);
//tmpl-kate-common-end
```